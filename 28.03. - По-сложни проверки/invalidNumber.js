function solve(num) {
    let number = Number(num);
    if ((number < 100 || number > 200)) {
        if (number != 0) {
        console.log("invalid")
    } else {
        console.log("");
    } } else {
        console.log("");
    } } 

solve(75);
solve(150);
solve(220);
solve(199);
solve(-1);
solve(100);
solve(200);
solve(0);